<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="Theme.HalfHashedKitty" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="android:windowLightNavigationBar">?attr/isLightTheme</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.HalfHashedKitty.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background -->
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <!-- Set your custom icon as the animated icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/halfhashedkitty2</item>
        <!-- Optional: set the duration of the splash screen icon animation -->
        <item name="windowSplashScreenAnimationDuration">500</item>
        <!-- Post-splash screen theme -->
        <item name="postSplashScreenTheme">@style/Theme.HalfHashedKitty</item>
    </style>
</resources>
